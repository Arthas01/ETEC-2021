#include <stdio.h>				
#include <conio.h>
#include <stdlib.h>
#include <locale.h>

int area, base, altura;

main(){

//-------------------------------
//area do retangulo -- fucionando

setlocale(LC_ALL,"");
printf("Digite a base:    ");
printf("Digite a altura:  ");
scanf("%d", &base);
scanf("%d", &altura);
area = base * altura;
printf("area = %d", area);
}
